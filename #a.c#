        j=t[i];

        if(i-1=>0)
              if(gClients[j].score!=gClients[j-1].score){
            sprintf(rank[i],"%d",i+1);
        bef=i+1;
         }
         else{
            rank[i]=rank[i-1]
                bef=
                sprintf(rank[i],"%d",bef);
         }